#' @param bw_net_range The range for the network bandwidth to consider, given as a numeric
#' vector of two values: c(bandwidth_min, bandwidth_max)
#' @param bw_net_step The step between each network bandwidth to calculate, given as a float
#' @param bw_time_range The range for the time bandwidth to consider, given as a numeric
#' vector of two values: c(bandwidth_min, bandwidth_max)
#' @param bw_time_step The step between each time bandwidth to calculate, given as a float
